Place your *.scorer & *.tflite files in this directory, and reference them in 'application.conf'.

Recommend as per README:
  model = "models/model.tflite"
  scorer = "models/large_vocabulary.scorer"
